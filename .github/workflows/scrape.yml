name: Playwright Scraper Workflow

on:
  push:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt
          playwright install

      # ✅ REQUIRED STEP WITH EMAIL
      - name: Run scraper - 23f1002421@ds.study.iitm.ac.in
        run: python main.py
